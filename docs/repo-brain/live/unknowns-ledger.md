# Unknowns Ledger

- Exact itch slug is not set.
- Visual asset direction is deferred; first release is prose-first with CSS.
- Generated `build/index.html` may be committed later if GitHub Pages becomes
  the publishing route.
- Accessibility pass is limited to smoke coverage for P0; deeper keyboard and
  contrast checks belong in the next release gate.
- The full Act 1 route is intentionally broad; the next product question is
  whether an itch build should expose all scenes up front or curate a shorter
  default path with the route map as an archive surface.
